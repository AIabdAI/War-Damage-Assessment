# Base deps — validation gate + data tooling (Python >= 3.10, verified on 3.12)
# Training-only deps (ultralytics, mlflow) live in requirements-train.txt.
dvc[gdrive]>=3.0        # ملفات .dvc بصيغة DVC 3 — الإصدار 2.x لا يقرؤها
numpy
pandas==2.3.3
matplotlib
opencv-python           # full build: annotation/review GUI tools need cv2.imshow
albumentations
Pillow==12.2.0
PyYAML==6.0.3
great-expectations==1.19.1
